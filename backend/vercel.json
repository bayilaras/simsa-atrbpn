{
    "version": 2,
    "buildCommand": "npm run build",
    "installCommand": "npm ci --include=dev",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/index.js"
        }
    ]
}
